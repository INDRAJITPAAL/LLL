<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Web App</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        .word {
            display: inline-block;
            margin: 1px;
            padding: 1px;
            opacity: 0;
            animation: fadeIn 0.3s ease-in-out forwards;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }
    </style>
</head>

<body>
    <div class="container col-11 col-sm-8 mt-2 overflow-auto overflow-md-visible bg-white">

        <div class="row">
            <% if (data) { %>
                <div class="response">
                    <h2>Response:</h2>
                    <div id="response-content" class="alert alert-success">
                        <% const words=data.split(' '); %>
                    <% words.forEach((word, index) => { %>
                        <span class="word" style="animation-delay: <%= (index * 0.03) %>s;">
                            <%= word %>
                          </span>
                                <% }); %>
                    </div>
                </div>
                <% } %>
        </div>
        


    </div>
    <form action="/send" method="POST" >
        <input type="text" name="userInput" placeholder="Enter your request" required>
        <button type="submit">Send</button>
    </form>
</body>

</html>